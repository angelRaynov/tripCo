{% extends 'base.html.twig' %}

{% block body_id 'register' %}

{% block main %}
    <h1>Hello, {{ user.name }}</h1>
    <img src="{{ asset('uploads/avatars/' ~ user.avatar) }}" width="350" height="200">
    <h1><a href="{{ path('user_cars') }}">My cars</a> </h1>
    <h1><a href="{{ path('user_update') }}">Update profile</a> </h1>
{% endblock %}

